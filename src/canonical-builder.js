/**
 * Função que recebe o canonical, o payload de carregamento e os dados processados e retorna o canonical modificado.
 *
 * @param {*} canonical 
 * @param {*} payloadDeCarregamento 
 * @param {*} processedData 
 * @returns 
 */
export const buildCanonical = (canonical, payloadDeCarregamento, processedData) => {

  if (!canonical) return;

  switch (canonical.name) {
    // faça algo com o canonico.
  }

  return data;
}